import { MgDirDirective } from './mg-dir.directive';

describe('MgDirDirective', () => {
  it('should create an instance', () => {
    const directive = new MgDirDirective();
    expect(directive).toBeTruthy();
  });
});
