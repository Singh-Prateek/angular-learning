import { NumericTextboxDirective } from './numeric-textbox.directive';

describe('NumericTextboxDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericTextboxDirective();
    expect(directive).toBeTruthy();
  });
});
